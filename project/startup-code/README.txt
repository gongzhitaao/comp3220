* HOWTO

  Start from the project folder,

  1. Create a building directory, separating from the code, (mkdir build && cd build)
  2. Build the whole project with CMake, (cmake ..)
  3. Find the program, (cd tests)
  4. Run the test, (./test-bf)
